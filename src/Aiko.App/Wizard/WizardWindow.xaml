<!-- The first run, in the same material as everything else Aiko shows. -->
<Window x:Class="Aiko.App.WizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Aiko.App"
        Title="{x:Static local:Strings.WizardWindowTitle}"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight">

  <local:WizardPanel x:Name="Panel" local:Motion.Appear="True" />
</Window>
