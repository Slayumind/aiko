<!--
  The settings window wears the same material as the card: its own frame, so every surface of
  Aiko looks like the same thing.
-->
<Window x:Class="Aiko.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Aiko.App"
        Title="{x:Static local:Strings.SettingsWindowTitle}"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight">

  <local:SettingsPanel x:Name="Panel" />
</Window>
